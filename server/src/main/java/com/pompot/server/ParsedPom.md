# ParsedPom

Simple data record describing a parsed pom file.

## Fields

- `String projectRoot` – Absolute path of the project directory that provided the pom.
- `JsonNode model` – JSON tree produced from the Maven model.
